<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
  </PropertyGroup>
  <ItemGroup>
    <!-- fork 同梱の Khronos loader を出力ディレクトリへ（DllImport は app dir から解決） -->
    <None Include="../../lib/openxr_loader.dll" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>
</Project>
